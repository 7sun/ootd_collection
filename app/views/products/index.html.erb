<h2>Products</h2>
<%= link_to "Add New", new_product_path %>
<% @products.each do |product| %>
	<h3><%= link_to product.style, product_path(product.id) %></h3>	
	<p><%= product.style_num %></p>
	<br>
<% end %>
