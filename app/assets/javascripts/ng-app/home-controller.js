angular
	.module("ootd")
  .controller("homeController", ["currentUser", '$scope', '$rootScope', function(currentUser, $scope, $rootScope){
  		// $rootScope.currentUser = currentUser;
  		// console.log("set current user: " + currentUser);
  		// console.log("rootscope user: " + $rootScope.currentUser);

	}]);